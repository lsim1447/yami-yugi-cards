{
  "name": "yami-yugi-cards",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "mongoose": "^5.9.13",
    "nodemon": "^2.0.3"
  },
  "scripts": {
    "start": "node ./server.js",
    "heroku-postbuild": "cd react-client && npm install && npm run build"
  }
}
